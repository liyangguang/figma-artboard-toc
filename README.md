A figma plugin to generate table of contents of all artboards, with links to each one.

## How to develop

The only thing needed is compile typescript.

In VSCode, "Terminal > Run Build Task...", `tsc: watch - tsconfig.json`
